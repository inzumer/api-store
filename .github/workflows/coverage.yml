name: Coverage Check and Comment

on:
  workflow_run:
    workflows: ["Lint"]
    types:
      - completed
  pull_request:
    branches:
      - main
      - develop

jobs:
  test-and-comment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run tests with coverage
        run: npm run test -- --coverage --coverageReporters=json-summary

      - name: Read coverage percentage
        id: coverage
        run: |
          # Extraemos el porcentaje de coverage de cobertura global
          coverage=$(jq '.total.lines.pct' coverage-summary.json)
          echo "coverage=$coverage" >> $GITHUB_OUTPUT

      - name: Comment coverage on PR
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            📊 **Test Coverage Report**
            La cobertura de líneas es de **${{ steps.coverage.outputs.coverage }}%**.

            <!-- Este comentario es automático. -->
